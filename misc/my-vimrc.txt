" set backup and swap files to /tmp, this works well on ubuntu or linux os
set backupdir=/tmp//
set directory=/tmp//
set undodir=/tmp//
" set indentation based on filetype on
filetype on
filetype indent on
" mark 80 col limit in red
set colorcolumn=80
" enable syntax highlights
syntax on
" search options
set ignorecase
set infercase
set smartcase
set incsearch
set hlsearch
set nowrapscan
" scrolling behaviour with h/j/k/l
set scrolloff=3               " keep at least x lines above/below
set sidescrolloff=3           " keep at least x lines left/right
" easy window navigation
map <C-j> <C-W>j<C-W>_
map <C-k> <C-W>k<C-W>_
map <C-h> <C-W>h<C-W>_
map <C-l> <C-W>l<C-W>_
" set windows min height in splits
set wmh=0                     " just display the filename & other info
" ease to reach ESC mode
nnoremap <SPACE> :
" show ruler info in the status bar
set ruler
" format code for c
set tabstop=4
set softtabstop=2
set shiftwidth=2
set expandtab
set cinoptions=(0
" show current mode
set showmode
" Make "show list" more useful.
set listchars+=tab:^-
set listchars+=trail:`
" ------------------- disabled options ------------------
" line numbering - *** disabled line numbering options since i'm anyhow using a ruler
"set number
"set numberwidth=4
" show cursor column for alignment - *** disabled as it's too jarring
"set cursorcolumn
